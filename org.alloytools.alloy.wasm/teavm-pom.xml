<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>org.alloytools</groupId>
    <artifactId>alloy-wasm-builder</artifactId>
    <version>6.0.0</version>
    <packaging>jar</packaging>

    <name>Alloy WASM Builder</name>
    <description>Maven project to build Alloy as WebAssembly using TeaVM</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <teavm.version>0.10.2</teavm.version>
    </properties>

    <dependencies>
        <!-- TeaVM dependencies -->
        <dependency>
            <groupId>org.teavm</groupId>
            <artifactId>teavm-classlib</artifactId>
            <version>${teavm.version}</version>
        </dependency>
        <dependency>
            <groupId>org.teavm</groupId>
            <artifactId>teavm-jso</artifactId>
            <version>${teavm.version}</version>
        </dependency>
        <dependency>
            <groupId>org.teavm</groupId>
            <artifactId>teavm-jso-apis</artifactId>
            <version>${teavm.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>17</source>
                    <target>17</target>
                </configuration>
            </plugin>
            
            <!-- TeaVM Maven Plugin -->
            <plugin>
                <groupId>org.teavm</groupId>
                <artifactId>teavm-maven-plugin</artifactId>
                <version>${teavm.version}</version>
                <executions>
                    <execution>
                        <id>generate-wasm</id>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                        <phase>package</phase>
                        <configuration>
                            <targetDirectory>${project.build.directory}/wasm</targetDirectory>
                            <mainClass>org.alloytools.alloy.wasm.AlloyWasm</mainClass>
                            <targetType>WEBASSEMBLY</targetType>
                            <minifying>true</minifying>
                            <debugInformationGenerated>false</debugInformationGenerated>
                            <sourceMapsGenerated>false</sourceMapsGenerated>
                            <sourceFilesCopied>false</sourceFilesCopied>
                        </configuration>
                    </execution>
                    <execution>
                        <id>generate-js</id>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                        <phase>package</phase>
                        <configuration>
                            <targetDirectory>${project.build.directory}/js</targetDirectory>
                            <mainClass>org.alloytools.alloy.wasm.AlloyWasm</mainClass>
                            <targetType>JAVASCRIPT</targetType>
                            <minifying>false</minifying>
                            <debugInformationGenerated>false</debugInformationGenerated>
                            <sourceMapsGenerated>false</sourceMapsGenerated>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
